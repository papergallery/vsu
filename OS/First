import time

# time.clock() измеряет количество процессорного времени, которое было использовано текущим процессом;
start = time.clock()
i = 1 + 1
elapsed = (time.clock() - start)
print(elapsed)

# time.time() будет возвращает секунды, прошедшие с эпохи.
start = time.time()
k = 1 + 1
elapsed = (time.time() - start)
print(elapsed)
